server:
  port: 8080

spring:
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.websocket.servlet.WebSocketServletAutoConfiguration

aws:
  dynamodb:
    enabled: true
  ses:
    enabled: true

logging:
  level:
    com.foodgroup: INFO
